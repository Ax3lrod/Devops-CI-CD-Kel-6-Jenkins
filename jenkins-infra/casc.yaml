jenkins:
  securityRealm:
    local:
      allowsSignup: false
      users:
       - id: "admin"
         password: "admin" # PASSWORD OTOMATIS
  authorizationStrategy:
    fullControlOnceLoggedIn:
      allowAnonymousRead: false

tool:
  go:
    installations:
    - name: "go1.22"
      properties:
      - installSource:
          installers:
          - "goInstaller":
              version: "1.22.12"

unclassified:
  location:
    url: "http://localhost:8080/"
